//Remota connection
sudo apt update
sudo apt upgrade

sudo apt install mysql-server
sudo su
root@ip-172-31-43-126:/home/ubuntu# passwd root
New password:
Retype new password: 
exit

sudo mysql_secure_installation
Y
sudo mysql -u root
mysql> ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password by '123';
mysql> flush privileges;
mysql> exit

sudo mysql -u root -p
mysql> create user 'favio'@'localhost' IDENTIFIED WITH mysql_native_password BY '123';
mysql> GRANT ALL PRIVILEGES ON *.* TO 'favio'@'localhost';

mysql> flush privileges;
mysql> exit

